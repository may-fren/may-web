# Docker Commands - may-web

## Build
docker build -t ozanemrahyakupoglu/may-web:1.0.2 .

## Push (Docker Hub)
docker push ozanemrahyakupoglu/may-web:1.0.2

## Run
docker run -d \
  --name may-web \
  -p 8091:8091 \
  ozanemrahyakupoglu/may-web:1.0.2

---

# Docker Compose Commands - may-web

## Build
docker compose build

## Build (with version)
VERSION=1.0.2 docker compose build

## Push (Docker Hub)
docker compose push

## Push (with version)
VERSION=1.0.2 docker compose push

## Run
docker compose up -d

## Run (with version)
VERSION=1.0.2 docker compose up -d
